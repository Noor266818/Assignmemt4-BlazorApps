@inherits LayoutComponentBase

<div class="app-canvas">
    <aside class="sidebar-container">
        <NavMenu />
    </aside>
    <main class="content-container">
        @Body
    </main>
</div>

<style>
    /* Reset overriding default layouts completely */
    .sidebar, .top-row {
        display: none !important;
    }

    .page {
        display: block !important;
    }

    .app-canvas {
        display: flex;
        min-height: 100vh;
        width: 100vw;
        position: fixed;
        top: 0;
        left: 0;
        background: #fff5f7; /* Milky bubblegum sweet pink canvas */
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    }

    .sidebar-container {
        width: 260px;
        min-width: 260px;
        height: 100vh;
        background: #fdf2f8;
        border-right: 2px solid #fbcfe8;
        z-index: 99;
    }

    .content-container {
        flex: 1;
        height: 100vh;
        display: flex;
        flex-direction: column;
        overflow-y: auto;
        background: #fff5f7;
    }
</style>