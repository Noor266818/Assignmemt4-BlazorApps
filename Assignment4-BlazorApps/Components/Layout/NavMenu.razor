<div class="premium-sidebar">
    <div class="brand-section">
        <div class="brand-logo">
            <span class="logo-icon">💖</span>
        </div>
        <span class="brand-name">GREETING.IO</span>
    </div>

    <div class="menu-section">
        <span class="menu-label">WORKSPACE</span>
        <a class="menu-item active" href="/">
            <span class="item-dot"></span> Overview Dashboard
        </a>
    </div>
</div>

<style>
    .premium-sidebar {
        display: flex;
        flex-direction: column;
        height: 100%;
        width: 100%;
        background: #170b14; /* Deep Velvet Dark Pink Base */
        color: #fce7f3;
    }

    .brand-section {
        padding: 28px 24px;
        display: flex;
        align-items: center;
        gap: 12px;
        border-bottom: 1px solid #37142c;
    }

    .brand-logo {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 32px;
        height: 32px;
        background: rgba(244, 63, 94, 0.15);
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(244, 63, 94, 0.2);
    }

    .logo-icon {
        font-size: 1rem;
        filter: drop-shadow(0 0 4px #f43f5e);
    }

    .brand-name {
        font-weight: 700;
        font-size: 0.95rem;
        letter-spacing: 1.5px;
        color: #ffffff;
        text-shadow: 0 0 8px rgba(244, 63, 94, 0.3);
    }

    .menu-section {
        padding: 24px 16px;
    }

    .menu-label {
        display: block;
        font-size: 0.7rem;
        font-weight: 700;
        color: #9d4b85; /* Soft dark pink for labels */
        letter-spacing: 1px;
        margin-bottom: 12px;
        padding-left: 12px;
    }

    .menu-item {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 12px 16px;
        border-radius: 10px;
        color: #f472b6; /* Soft light pink text */
        text-decoration: none;
        font-size: 0.9rem;
        font-weight: 500;
        transition: all 0.2s ease;
    }

        .menu-item.active {
            background: #2b0f24;
            color: #f43f5e; /* Hot Pink text */
            font-weight: 600;
            border: 1px solid #50133e;
            box-shadow: inset 0 0 8px rgba(244, 63, 94, 0.15);
        }

        .menu-item:hover:not(.active) {
            background: #3c1231;
            color: #ffffff;
        }

    .item-dot {
        width: 6px;
        height: 6px;
        background: currentColor;
        border-radius: 50%;
        box-shadow: 0 0 6px currentColor;
    }
</style>